import { FormulaireConnexion } from '@/components/auth/formulaire-connexion';

export const metadata = {
  title: 'Connexion · TBB Fashion',
};

export default function ConnexionPage() {
  return <FormulaireConnexion />;
}